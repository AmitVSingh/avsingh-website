# Portfolio V2

Edit `content.json` for routine text updates. Keep the JSON punctuation intact.

Replace these files without changing their names:

- `assets/amit-singh.jpg` — profile image
- `assets/airy-plasmon.png` — research image
- Update the CV URL in `index.html` only if its location changes. The current button uses the CV already hosted on the original website.

Commit and push changes to the `portfolio-v2` branch. Netlify will publish them automatically once the branch is connected.

Netlify settings: branch `portfolio-v2`, no build command, publish directory `portfolio-v2`.
